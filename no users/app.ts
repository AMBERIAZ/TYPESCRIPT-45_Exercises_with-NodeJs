//assignment no : 31
let userNames: string[] = [];

if (userNames.length > 0) {
    console.log("We have enough users");
} else {
    console.log("We need to find some users!");

}
// i removed  all usernames of previous assignment so the message "We need to find some users!" is printed.